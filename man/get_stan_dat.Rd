% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonical_dcm_functions.R
\name{get_stan_dat}
\alias{get_stan_dat}
\title{Construct Stan data list for CDCM}
\usage{
get_stan_dat(
  data,
  hypothesis_idxs,
  ode_solver_type = 1,
  tol = 10^{
     -5
 },
  sigma_nu = 1,
  sigma_nu_self = 0.125,
  rate_sigma = 0.5,
  sigma_z0 = 0.3,
  conv = 1,
  max_num_steps = 10^6
)
}
\arguments{
\item{data}{A list containing:
\itemize{
\item \code{times}: Numeric vector of time points (length T)
\item \code{u}: Matrix of stimulus inputs (T x n_u)
\item \code{y_obs}: Matrix of observed BOLD signals (T x m)
}}

\item{hypothesis_idxs}{A list specifying model structure:
\itemize{
\item \code{A_idxs}: Matrix of indices for A matrix (connections)
\item \code{B_idxs}: Matrix of indices for B matrix (modulatory effects)
\item \code{C_idxs}: Matrix of indices for C matrix (inputs)
}}

\item{ode_solver_type}{Integer specifying ODE solver (default 1 is piecewise analytic solution)}

\item{tol}{Numeric tolerance for ODE solver (default 1e-5, if using numeric CKRK integration)}

\item{sigma_nu}{Prior scale for connectivity parameters}

\item{sigma_nu_self}{Prior scale for self-connections in A and B}

\item{rate_sigma}{Prior rate parameter for variance terms}

\item{sigma_z0}{Prior scale for initial neural states}

\item{conv}{Integer flag for convolution option (default 1 convolves with canonical HRF - recommended to not change unless you want latent neural signal)}

\item{max_num_steps}{Maximum number of ODE solver steps (for CKRK solver)}
}
\value{
A named list suitable for input to a Stan model.
}
\description{
Prepares input data and model specification into the list format required
for the canonical DCM Stan program. This includes time series data, stimulus
inputs, connectivity structure indices, and hyperparameters.
}
