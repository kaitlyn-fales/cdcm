% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_cdcm.R
\name{simulate_cdcm}
\alias{simulate_cdcm}
\title{Simulate fMRI time series under CDCM}
\usage{
simulate_cdcm(
  nu,
  hypothesis_idxs,
  nscan,
  m,
  n_u,
  TR,
  U,
  SNR,
  reparam = TRUE,
  z0 = NULL,
  check_solution = TRUE,
  tol = 1e-04,
  seed = NULL
)
}
\arguments{
\item{nu}{A list containing true parameter values:
\describe{
\item{nu_A}{numeric vector of A (baseline connectivity) parameters}
\item{nu_B}{numeric vector of B (modulatory) parameters}
\item{nu_C}{numeric vector of C (input) parameters}
}}

\item{hypothesis_idxs}{A list specifying the locations of nonzero parameters:
\describe{
\item{A_idxs}{matrix with 2 columns (row, column indices)}
\item{B_idxs}{matrix with 3 columns (input, row, column indices)}
\item{C_idxs}{matrix with 2 columns (row, input indices)}
}}

\item{nscan}{Number of scan time points.}

\item{m}{Number of brain regions (state dimension).}

\item{n_u}{Number of experimental inputs.}

\item{TR}{Repetition time (seconds).}

\item{U}{Input (stimulus) matrix of dimension \verb{nscan x n_u} or \verb{(nscan + 1) x n_u}.
Values must be binary (0/1).}

\item{SNR}{Numeric. Signal-to-noise ratio for additive Gaussian noise,
defined as the ratio of the standard deviation of the signal to that of
the noise (i.e., \eqn{\mathrm{SNR} = \mathrm{sd}(\text{signal}) / \mathrm{sd}(\text{noise})}).
Noise is generated such that
\eqn{\mathrm{sd}(\text{noise}) = \mathrm{sd}(\text{signal}) / \mathrm{SNR}},
implying a noise variance of
\eqn{\sigma^2 = \mathrm{Var}(\text{signal}) / \mathrm{SNR}^2}.
Must be positive.}

\item{reparam}{Logical. If \code{TRUE}, reparameterizes diagonal elements of the A matrix
to enforce stability.}

\item{z0}{Optional initial state vector. If \code{NULL}, a default is used.}

\item{check_solution}{Logical. If \code{TRUE}, compares analytic and numeric ODE solutions
to assess numerical stability.}

\item{tol}{Numeric tolerance for the analytic vs numeric solution comparison.}

\item{seed}{Optional seed (scalar or length \code{m}) for reproducible noise generation.}
}
\value{
A list with two components:
\describe{
\item{simulated_data}{
\describe{
\item{times}{Vector of scan times (length \code{nscan})}
\item{u}{Input matrix at scan times (\verb{nscan x n_u})}
\item{y_obs}{Observed BOLD signals (\verb{nscan x m})}
}
}
\item{true_signals}{
\describe{
\item{z_signal}{Latent neural states (\verb{nscan x m})}
\item{y_signal}{Noise-free BOLD signals (\verb{nscan x m})}
}
}
}
}
\description{
Simulates neural state trajectories and observed BOLD signals under a
CDCM with user-specified connectivity parameters and experimental inputs.
}
\details{
This function generates:
\itemize{
\item latent neural states via an analytic solution to a bilinear state equation
\item observed BOLD signals via convolution with a canonical hemodynamic response function (HRF)
\item additive Gaussian noise controlled by a specified signal-to-noise ratio (SNR)
}

The user supplies the connectivity structure through \code{nu} and \code{hypothesis_idxs},
along with a stimulus design matrix \code{U}. Internally, the function augments the
input matrix to include the initial time point required for solving the system.

The neural dynamics follow the bilinear state space DCM. Within each block of
constant input, the system admits a closed-form solution, which is used here
for efficient simulation. The observed BOLD signal is obtained by convolving
the neural states with the canonical HRF (linear combination of two gamma distributions).
}
\examples{
nu <- list(
  nu_A = c(0.4, 0.3, -0.1, 0.15),
  nu_B = c(-0.2),
  nu_C = c(0.7)
)

hypothesis_idxs <- list(
  A_idxs = matrix(c(2,1,
                    1,2,
                    1,1,
                    2,2), byrow = TRUE, ncol = 2),
  B_idxs = matrix(c(1,2,1), byrow = TRUE, ncol = 3),
  C_idxs = matrix(c(1,1), byrow = TRUE, ncol = 2)
)

U <- matrix(c(0,rep(c(rep(0,5),rep(1,5)), 2)), ncol = 1)

sim <- simulate_cdcm(
  nu = nu,
  hypothesis_idxs = hypothesis_idxs,
  nscan = 20,
  m = 2,
  n_u = 1,
  TR = 2,
  U = U,
  SNR = 5,
  seed = 1
)

}
