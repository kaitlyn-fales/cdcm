% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_model.R
\name{compile_canonical_dcm}
\alias{compile_canonical_dcm}
\title{Compile the canonical DCM Stan model}
\usage{
compile_canonical_dcm(force_recompile = FALSE, quiet = TRUE, ...)
}
\arguments{
\item{force_recompile}{Logical; if \code{TRUE}, forces recompilation of the
Stan model even if a compiled executable already exists.}

\item{quiet}{Logical; passed to \code{cmdstanr::cmdstan_model()} to control
console output during compilation.}

\item{...}{Additional arguments passed to \code{cmdstanr::cmdstan_model()}.}
}
\value{
A \code{CmdStanModel} object.
}
\description{
Compiles the canonical Dynamic Causal Model (DCM) Stan program bundled
with the \code{cdcm} package using \code{cmdstanr}.
}
\details{
This function provides a convenient interface for compiling the model
without requiring users to manually manage file paths to the Stan program.

This function requires a working CmdStan installation (version 2.35.0 or
newer) configured for use with \code{cmdstanr}. If CmdStan is not installed
or configured, an error will be thrown.
}
\examples{
\dontrun{
mod <- compile_canonical_dcm()

# Force recompilation if needed
mod <- compile_canonical_dcm(force_recompile = TRUE)
}

}
